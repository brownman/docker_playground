alias grep1="grep --exclude-dir='.*'"
commander() 
{ 
    local args=($@);
    local cmd="${args[@]}";
    trace cmd: $cmd;
    eval "$cmd"
}

trace() 
{ 
    echo "[-] $@" 1>&2
}

export -f trace
export -f commander
