machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - ls -l ./test/Dockerfile.scratch
    #- docker build -t scratch1 ./test/scratch
    #- docker build -t runner - < Dockerfile.runner
    
    - docker build -t runner - < ./test/Dockerfile.scratch
